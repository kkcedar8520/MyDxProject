

sprite 클래스를 만들어 sprite 움직에 대한 flag 값과 함수들을 사용한다.


SetAnImation// 애니메이션에 걸리는 시간 지속시간 스프라이트 정보들을 세팅한다.

SetTextureArray //스프라이트 파일이 가지고있는 배열에대한 정보를 저장한다.
텍스쳐의 이름을 저장한다 


Rect 구조체는 내부 변수가 정수값으로 이뤄져있으므로 
새로 구조체를 만든다.
D3DXVECTOR2 uv[4];  //x,y 값을 가진 점 4개로 사각형을 만든다;
